<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class="mt-4">
        <div class="row">
            <div class="col-sm-6 col-lg-3">
                <div class="card card-sensor-item" >
                    {{!-- <a href="/area/{{this.slug}}">
                        <img src="{{this.image}}" class="card-img-top" alt="{{this.name}}"> 
                    </a> --}}
                    <div class="card-body">
                        <a href="/area/{{this.slug}}">
                            <h5 class="card-title">{{this.name}}</h5>
                        </a>
                      <p class="card-text">{{this.description}}</p>
                      <p class="card-text" >Topic: {{this.topic}}</p>
                      <h5>Nhiệt độ:<span id="temp-{{this.topic}}"></span></h1>
                      <h5>Độ ẩm:<span id="humi-{{this.topic}}"></span></h1>
                      <h5>Độ ẩm đất:<span id="soil-{{this.topic}}"></span></h1>
                      <a href="/area/{{this.slug}}" class="btn btn-success">Chi tiết</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>